name: calendar
description: "飞书日历日程管理（创建/查询/更新事件、freebusy 等）。auth 由 _constructor 自动处理。"
required_scope: "calendar:calendar calendar:calendar:read calendar:calendar.event:create calendar:calendar.event:read calendar:calendar.event:update calendar:calendar.event:delete calendar:calendar.free_busy:read"
triggers:
  - "日历"
  - "日程"
  - "calendar"
  - "创建会议"
  - "查日程"
input:
  open_id: string
  action: string
  scope: string
steps:
  - name: execute
    type: code
    command: "node steps/execute.mjs"
output: execute

name: image-ocr
description: "图片 OCR 识别（应用级 tenant token；需后台开通 optical_char_recognition:image）。"
required_scope: ""
triggers:
  - "OCR"
  - "图片识别"
  - "ocr image"
input:
  open_id: string
  action: string
  image: string
  scope: string
steps:
  - name: execute
    type: code
    command: "node steps/execute.mjs"
output: execute

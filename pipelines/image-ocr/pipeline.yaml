name: image-ocr
description: "图片 OCR 识别（仅需 tenant token）。auth 由 _constructor 自动处理。"
triggers:
  - "OCR"
  - "图片识别"
  - "ocr image"
input:
  open_id: string
  action: string
  scope: string
steps:
  - name: execute
    type: code
    command: "node steps/execute.mjs"
output: execute

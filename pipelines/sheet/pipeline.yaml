name: sheet
description: "飞书电子表格操作（读写单元格、范围、公式）。auth 由 _constructor 自动处理。"
triggers:
  - "电子表格"
  - "sheet"
  - "读取表格"
input:
  open_id: string
  action: string
  scope: string
steps:
  - name: execute
    type: code
    command: "node steps/execute.mjs"
output: execute
